services:
  rtdb-emulator:
    volumes:
      - ../functions/.env.local:/app/functions/.env:ro
    environment:
      NODE_OPTIONS: "--trace-uncaught"
    ports:
      - "4000:4000"
      - "9000:9000"
      - "9099:9099"
      - "5001:5001"
    profiles: ['integration-test']
